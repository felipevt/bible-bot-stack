FROM n8nio/n8n:latest

# Criar diretório de workflows se não existir
RUN mkdir -p /home/node/.n8n/workflows

# Copiar workflows personalizados (se existirem)
# COPY ./workflows /home/node/.n8n/workflows

# Expor porta
EXPOSE 5678

# O comando padrão já está definido na imagem base